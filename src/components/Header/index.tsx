import { HeaderContainer } from './styles';

const Header = () => {
    return (
        <HeaderContainer>
            Where in the World?
        </HeaderContainer>
     );
}

export default Header;
